name: Whisparr v3

on:
  push:
    paths:
      - .github/workflows/whisparr3.yml
      - contexts/whisparr3/*
  pull_request:
    paths:
      - .github/workflows/whisparr3.yml
      - contexts/whisparr3/*
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  # renovate: datasource=github-releases depName=Whisparr/Whisparr-Eros versioning=loose
  WHISPARR_VERSION: "3.6.2-release.1727"

jobs:
  versions:
    name: Versions
    outputs:
      WHISPARR_VERSION: ${{ env.WHISPARR_VERSION }}
    permissions:
      contents: none
    runs-on: ubuntu-26.04
    steps:
      - name: Generate outputs
        run: |
          echo "WHISPARR_VERSION=${{ env.WHISPARR_VERSION }}" >> $GITHUB_OUTPUT

  build-publish:
    name: Call Build
    needs: versions
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
      security-events: write
    uses: oliversalzburg/workflows/.github/workflows/publish-oci.yml@0dda09981f62a30b1120ffb1cfb68bd07f799def # main
    with:
      build-args: |
        WHISPARR_VERSION=${{ needs.versions.outputs.WHISPARR_VERSION }}
      context: contexts/whisparr3
      dist-tag: latest
      image-description: Works great and looks even better doing it.
      image-name: oliversalzburg/containers/whisparr
      image-tag: v${{ needs.versions.outputs.WHISPARR_VERSION }}
      image-title: Whisparr v3
      is-latest: true
      push: ${{ github.event_name == 'push' && github.ref_name == 'main' }}
      with-sarif: false
      with-sbom: false
